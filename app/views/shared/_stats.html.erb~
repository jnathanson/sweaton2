<% @user ||= current_user %>
<div class="stats">
  <a href="<%= favourites_user_path(@user) %>">
    <strong id="Favourites" class="stat">
      <%= @user.venues.count %>
    </strong>
    <%= pluralize(@user.venues.count, "favourite") %>
  </a>
  <a href="<%= attendings_user_path(@user) %>">
    <strong id="Attending" class="stat">
      <%= @user.events.count %>
    </strong>
    attending
  </a>
</div>

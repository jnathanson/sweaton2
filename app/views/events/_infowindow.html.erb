<style>
  .squirtle {
      text-align: left;
      background-color: #f7931e;
      color: #00ff00;
    h1, p {
      color: #ff0000;
    }
  }
</style>
<div id="squirtle">
  <h1><%= event.name %></h1>
  <p>at <%= event.venue.name %></p>
  <p><%= event.start_time.to_s(:time_day) %>-<%= event.end_time.to_s(:time_day) %>
  <p><%= event.description %></p>
  <%= link_to "JOIN ACTIVITY", '#' %>
</div>

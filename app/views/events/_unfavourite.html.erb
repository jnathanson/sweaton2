<%= form_for(current_user.favourites.find_by(event_id: @event),
             html: { method: :delete }, remote: true) do |f| %>
  <%= image_tag 'tag_icons/favourite.png' %>
  <%= f.submit "Unfavourite", class: "btn btn-large" %>
<% end %>

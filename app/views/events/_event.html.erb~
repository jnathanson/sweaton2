<li>
  <span class="name and date"><%= link_to event.name, event_path(event.id) %> <%= event.start_time.to_s(:time_day) %>, <%= ((event.end_time-event.start_time)/1.hour).round %> <%= %></span>
  <span class="description"><%= event.description %></span>
</li>

<li>
  <span class="name and date"><%= link_to event.name, event_path(event.id), 'data-no-turbolink => true' %> <%= event.start_time.to_s %>-<%= event.end_time.to_s %></span>
  <span class="description"><%= event.description %></span>
</li>

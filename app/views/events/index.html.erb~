<% provide(:title, 'All events') %>
<% if @number==nil
     str = ""
   else
     str = "at "+Venue.find(@number).name
   end %>
<h1>All events <% str %></h1>

<%= render 'shared/search' %>

<% if @events.any? %>

  <ul class="events">
    <%= render @events, size: 52 %>
  </ul>

<% else %>

  <p>No events listed for this venue.</p>

<% end %>

<%= form_for(current_user.attendings.find_by(event_id: @event),
             html: { method: :delete }, remote: true) do |f| %>
  <div><%= f.hidden_field :event_id %></div>
  <%= image_tag 'tag_icons/not_favourite.png' %>
  <%= f.submit "Attend all", class: "btn btn-large btn-primary" %>
<% end %>

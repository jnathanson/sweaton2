<%= form_for(current_user.relationships.find_by(event_id: @event),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unattend", class: "btn btn-large" %>
<% end %>
